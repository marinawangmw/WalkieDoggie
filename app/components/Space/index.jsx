import React from 'react';
import { View } from 'react-native';
import StyleSheetFactory from '../AuthenticationContent/Login/styles';

const Space = ({ horizontal, vertical, space }) => {
  return <View />;
};

export default Space;
