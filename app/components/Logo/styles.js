import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginBottom: 10,
  },
  logo: {
    height: 90,
    width: 180,
    alignSelf: 'center',
    resizeMode: 'cover',
  },
});

export default styles;
